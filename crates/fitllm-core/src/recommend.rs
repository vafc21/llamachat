//! Recommendation engine. STUB — replaced by the recommend agent.

use crate::types::*;

pub fn rate_all(
    _profile: &HardwareProfile,
    _catalog: &ModelCatalog,
    _benchmarks: &[BenchmarkResult],
) -> Vec<Recommendation> {
    vec![]
}
